# deca joins - 海浪
<div style={{
    maxWidth: '100%',
    position: 'relative',
    paddingTop: '56.25%' /* 16:9 比例 */
}}>
    <ReactPlayer
        url="/video/wave.mp4"
        playing
        controls
        playsinline       // 关键属性：iOS 内联播放
        webkit-playsinline // 旧版 iOS 支持
        muted              // 允许自动播放（可选）
        width="100%"
        height="100%"
        style={{
            position: 'absolute',
            top: 0,
            left: 0,
            objectFit: 'cover' // 保持视频比例
        }}
        config={{
            file: {
                attributes: {
                    playsinline: '' // 兼容更多浏览器
                }
            }
        }}
    />
</div>